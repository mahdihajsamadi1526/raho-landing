{
  "name": "raho-landing-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing landing page for the Raho app (راهو).",
  "scripts": {
    "validate": "node scripts/validate.js",
    "headless": "node scripts/headless-check.js",
    "check": "node scripts/validate.js && node scripts/headless-check.js"
  },
  "devDependencies": {
    "puppeteer": "^25.1.0"
  }
}
